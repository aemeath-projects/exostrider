/**
 * 会话特殊命令常量 —— 全局取消与确认命令定义。
 */

/** 默认取消命令列表。 */
export const DEFAULT_CANCEL_COMMANDS: readonly string[] = ['/取消', '/cancel']

/** 默认确认命令列表。 */
export const DEFAULT_CONFIRM_COMMANDS: readonly string[] = ['/确认', '/confirm']
